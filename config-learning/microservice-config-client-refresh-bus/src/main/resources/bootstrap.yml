spring:
  application:
      name: foobar
  cloud:
    config:
      uri: http://localhost:8040
      profile: dev 
      label: master
    bus:
      trace:
        enabled: true   # 为了能看到bus的监控端点
        
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest